import React from 'react'
import { clinic } from '../assets'

const Contact2 = () => {
  return (
    <div className={`w-full `}>
        <img className={`w-full`} src={clinic}></img>
    </div>
  )
}

export default Contact2